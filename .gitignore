node_modules
.next

# Keep environment variables out of version control
.env
.env.local
.env*.local

/src/generated/prisma

# OS / editor junk
.DS_Store

# One-off DB repair backups (created during data fixes — never commit)
_backup_*.json
